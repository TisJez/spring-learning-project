spring.application.name=practiceproj

spring.datasource.initialize=true

# Database URL, username, and password
spring.datasource.url=jdbc:mysql://localhost:3306/myDatabase"
spring.datasource.username=root
spring.datasource.password=s3cr3t

# Additional configurations (optional)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update  # Automatically updates schema based on entity classes
spring.jpa.show-sql=true              # Prints SQL statements in the console